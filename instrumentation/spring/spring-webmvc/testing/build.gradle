apply from: "$rootDir/gradle/java-conventions.gradle"

dependencies {
  compileOnly("org.springframework.boot:spring-boot-starter-web:${versions.springboot}")
  compileOnly("org.springframework.boot:spring-boot-starter-test:${versions.springboot}")
}
